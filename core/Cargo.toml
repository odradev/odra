[package]
name = "odra"
version = "0.0.1"
edition = "2021"
authors = ["Jakub Płaskonka <kuba@odra.dev>", "Krzysztof Pobiarżyn <krzysztof@odra.dev>", "Maciej Zieliński <maciej@odra.dev>"]
license = "MIT"
repository = "https://github.com/odradev/odra"
description = "Utilites uses by the Odra Casper-related crates."
keywords = ["wasm", "webassembly", "blockchain"]
categories = ["wasm", "smart contracts"]

[dependencies]
odra-types = { path = "../types" }
odra-utils = { path = "../utils" }
odra-proc-macros = { path = "../lang/proc_macros" }
odra-mock-vm = { path = "../mock_vm", optional = true }
odra-test-env-wrapper = { path = "../test_env_wrapper", optional = true  }
ref_thread_local = "0.1.1"
cfg-if = "1.0.0"

[features]
default = [ "mock-vm" ]
mock-vm = [ "odra-mock-vm" ]
wasm = [ ]
wasm-test = [ "odra-test-env-wrapper" ]